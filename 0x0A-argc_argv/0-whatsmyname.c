#include <stdio.h>

/**
 * main - program that prints it's name
 * @argc: argument count
 * @argv: argument array
 *
 * Return: Always 0
 */

int main(__attribute__((unused))int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}

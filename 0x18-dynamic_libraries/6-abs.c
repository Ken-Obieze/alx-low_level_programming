#include "main.h"

/**
 * _abs - computes absolute value of integers
 *@a: int to compute absolute value
 * Return: Always 0
 */

int _abs(int a)
{
	if (a >= 0)
		return (a);
	else
		return (a * -1);

}

